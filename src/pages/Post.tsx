import React from 'react';
import styles from './Post.module.css';

const Post: React.FC = () => {
    return (
        <div className={styles.container}>
        </div>
    );
};

export default Post;